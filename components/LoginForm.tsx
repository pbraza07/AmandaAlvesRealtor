"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export function LoginForm(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const r=useRouter();return <form onSubmit={async e=>{e.preventDefault();setBusy(true);setError("");const res=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});const j=await res.json();setBusy(false);if(!res.ok)return setError(j.error);r.push("/admin");r.refresh()}} className="login-card"><span className="eyebrow">Private access</span><h1>Welcome back, Amanda.</h1><p>Sign in to manage inquiries and website content.</p><div className="stack"><label>Email<input className="admin-input" style={{width:"100%"}} type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><label>Password<input className="admin-input" style={{width:"100%"}} type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<div className="error">{error}</div>}<button className="button dark" disabled={busy}>{busy?"Signing in…":"Sign in"}</button><a href="/admin/forgot-password" style={{textAlign:"center"}}>Forgot password?</a><a href="/" style={{textAlign:"center"}}>Return to website</a></div></form>}
