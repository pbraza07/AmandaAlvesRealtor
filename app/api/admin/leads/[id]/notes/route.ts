import { NextRequest,NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { clean,requireAdmin,verifyOrigin } from "@/lib/security";
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();await verifyOrigin();const {id}=await params;const {body}=await req.json();const value=clean(body,5000);if(!value)return NextResponse.json({error:"Note is required"},{status:400});const note=await prisma.leadNote.create({data:{leadId:id,body:value}});await prisma.leadActivity.create({data:{leadId:id,action:"Internal note added"}});return NextResponse.json(note)}catch{return NextResponse.json({error:"Unable to add note"},{status:400})}}
