import { notFound,redirect } from "next/navigation";import { getAdmin } from "@/lib/security";import { prisma } from "@/lib/prisma";import { AdminNav } from "@/components/AdminNav";import { LeadDetail } from "@/components/LeadDetail";
export default async function LeadPage({params}:{params:Promise<{id:string}>}){if(!await getAdmin())redirect("/admin/login");const {id}=await params;const lead=await prisma.lead.findUnique({where:{id},include:{activities:{orderBy:{createdAt:"desc"}},notes:{orderBy:{createdAt:"desc"}},tasks:{orderBy:{createdAt:"desc"}},emails:{orderBy:{createdAt:"desc"}},files:true}});if(!lead)notFound();return <div className="admin-shell"><AdminNav/><main className="admin-main"><LeadDetail lead={JSON.parse(JSON.stringify(lead))}/></main></div>}
